# Flask 앱(app 패키지)이 쓰는 의존성 목록.
# Lambda(api 패키지)는 이 중 아무것도 필요 없다 - 표준 라이브러리만 쓴다.
# 단, Lambda 에서 DB 적재/알람까지 하려면 배포 패키지에 psycopg 와 boto3 가 필요하다.
Flask==3.0.3
python-dotenv==1.0.1

# CSRF 방어. 폼 POST 에 토큰을 요구한다.
# 없으면 로그인된 사용자가 남의 페이지를 여는 것만으로 알람 확인·런북 저장·
# 작업 승인 같은 쓰기가 실행될 수 있다(cross-site request forgery).
Flask-WTF==1.2.1
# Bedrock 을 쓰려면 [bedrock] 이 필요하다(botocore 설치 - SigV4 서명용).
# Claude API 만 쓸 거면 anthropic==0.125.0 로 줄여도 된다.
anthropic[bedrock]==0.125.0
boto3==1.35.99

# DB 접속용. Lambda 로컬 모드와 flask init-db 가 사용한다.
psycopg[binary]==3.2.3

# 리포트를 PowerPoint 로 내보낼 때 사용.
python-pptx==1.0.2

# 컴플라이언스 점검 결과를 엑셀로 내보낼 때 사용.
# 새 파일을 쓰기만 하므로 openpyxl 대신 이쪽을 쓴다(서식 지정이 간단하다).
# 없어도 앱은 뜬다 - 엑셀 내려받기만 안내를 띄우고 막힌다.
XlsxWriter==3.2.9

# 테스트 (python -m pytest). 앱 실행에는 필요 없다.
pytest==9.1.1
# 엑셀 내보내기 테스트가 만든 파일을 다시 열어볼 때 쓴다.
# 쓰는 쪽(XlsxWriter)과 읽는 쪽을 다른 것으로 두어야, 한쪽의 버그를
# 다른 쪽이 덮어주지 않는다. 없으면 그 테스트만 건너뛴다.
openpyxl==3.1.5
